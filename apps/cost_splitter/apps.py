from django.apps import AppConfig


class CostSplitterConfig(AppConfig):
    """AppConfig for the cost_splitter app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.cost_splitter"
    verbose_name = "Cost Splitter"
